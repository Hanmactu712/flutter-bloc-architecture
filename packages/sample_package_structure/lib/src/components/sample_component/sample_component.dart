import 'package:flutter/material.dart';

class SampleComponent extends StatelessWidget {
  const SampleComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
