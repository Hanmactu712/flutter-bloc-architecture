name: app_auth
description: app authentication & authorization package.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.6.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  go_router: ^16.2.0
  logging: ^1.3.0
  bloc: ^9.0.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  cached_network_image: ^3.4.1
  url_strategy: ^0.3.0
  url_launcher: ^6.3.2

  intl: ^0.19.0
  flutter_localizations:
    sdk: flutter

  uuid: ^4.5.1

  shared_preferences: ^2.5.3

  app_core:
    path: ../app_core
  font_awesome_flutter: ^10.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
